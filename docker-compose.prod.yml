version: "3.9"

services:
  backend:
    image: planetarium-backend:latest
    ports:
      - "3310:3310"
    environment:
      - NODE_ENV=production

  frontend:
    image: planetarium-frontend:latest
    ports:
      - "8080:80"
    depends_on:
      - backend